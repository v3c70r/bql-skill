# BQL Skill Research Project Dependencies

# Core
pyyaml>=6.0

# BQL execution (optional, uses CLI fallback if unavailable)
beanquery>=0.1.0
beancount>=2.3.0

# Testing
pytest>=7.0
pytest-cov>=4.0

# HTTP / Scraping (for corpus collection)
requests>=2.28

# Data processing
python-dateutil>=2.8
